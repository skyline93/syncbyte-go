syntax = "proto3";

option go_package = "internal/proto";

package proto;

service SyncbyteService {
    rpc Call (Request) returns (Response) {}
}

message Request {
    string action = 1;
    bytes options = 2;
}


message Response {
    bytes result = 1;
}
